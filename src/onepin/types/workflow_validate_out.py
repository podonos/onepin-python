# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .validation_blocked_out import ValidationBlockedOut
from .validation_detail_out import ValidationDetailOut


class WorkflowValidateOut(UniversalBaseModel):
    """
    Result of validating a definition against the full run-start bundle, without saving it.

    `valid` reflects `errors` only — a non-empty `blocked` does not flip it to `False`,
    since `blocked` entries are not graph defects (see `ValidationBlockedOut`).
    """

    valid: bool = pydantic.Field()
    """
    True only when `errors` is empty. Not affected by `blocked`.
    """

    summary: typing.Dict[str, typing.Any] = pydantic.Field()
    """
    Presentation summary of the definition (see `build_definition_summary`); populated on both outcomes.
    """

    errors: typing.Optional[typing.List[ValidationDetailOut]] = pydantic.Field(default=None)
    """
    Structural / run-start rule violations. Editable by changing the graph.
    """

    warnings: typing.Optional[typing.List[ValidationDetailOut]] = pydantic.Field(default=None)
    """
    Non-blocking structural warnings (e.g. `cycle`). Never affects `valid`.
    """

    blocked: typing.Optional[typing.List[ValidationBlockedOut]] = pydantic.Field(default=None)
    """
    Entitlement refusals. Not fixable by editing the graph.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
