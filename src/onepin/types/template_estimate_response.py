# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .node_estimate import NodeEstimate
from .template_estimate_response_cache_status import TemplateEstimateResponseCacheStatus
from .template_estimate_response_source_snapshot import TemplateEstimateResponseSourceSnapshot


class TemplateEstimateResponse(UniversalBaseModel):
    """
    Per-unit pricing guide for a visible template snapshot.

    Templates do not contain the caller's final script. Costs are therefore
    quoted for a fixed input-character unit plus fixed workflow overhead; cloned
    workflows still use `EstimateResponse` once real script text exists.
    """

    unit_chars: int
    variable_min_credits_per_unit: int
    variable_expected_credits_per_unit: int
    variable_max_credits_per_unit: int
    fixed_min_credits: int
    fixed_expected_credits: int
    fixed_max_credits: int
    total_min_credits_per_unit: int
    total_expected_credits_per_unit: int
    total_max_credits_per_unit: int
    breakdown: typing.List[NodeEstimate]
    source_snapshot: TemplateEstimateResponseSourceSnapshot
    source_node_ids: typing.List[str]
    definition_fingerprint: str
    rate_fingerprint: str
    cache_status: TemplateEstimateResponseCacheStatus
    computed_at: dt.datetime

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
