# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .workflow_run_analysis_credit_row_out import WorkflowRunAnalysisCreditRowOut
from .workflow_run_analysis_credits_out_reason import WorkflowRunAnalysisCreditsOutReason
from .workflow_run_analysis_credits_out_status import WorkflowRunAnalysisCreditsOutStatus


class WorkflowRunAnalysisCreditsOut(UniversalBaseModel):
    """
    Per-node credit breakdown for the run.

    ``charged`` is what the run COST the customer: the credits settlement debited from the
    balance plus ``overage_credits``, the remainder billed against the next invoice when
    the balance ran out mid-run. A Free plan's shortfall is absorbed by donut, emits no
    overage event, and so is excluded from both.

    ``truncated`` marks a response where at least one node's line union hit the server-side
    cap. Row amounts still come from the ledger and stay exact; only the ``free`` rows
    reached via the observed-work signal are best-effort on such a response.

    ``minimum_charge_fractional`` is the credits the per-run 1-credit floor added to what the
    run ACTUALLY paid — non-zero only when fractional usage was under 1 credit and the run-level
    minimum rounded it UP. It is the mirror of ``absorbed_fraction`` (the round-*down* amount when
    usage ≥ 1 credit) and is BOUNDED by ``charged``, so an absorbed Free-plan run (which settles
    below the nominal floor, ``charged`` as low as 0) never claims an uncollected minimum. It lets
    a client explain a sub-1 total that settled to 1 as a minimum charge rather than a bare
    ``0.5 → 1`` up-arrow. Null when the run is not settled (``charged`` is null).
    """

    status: WorkflowRunAnalysisCreditsOutStatus
    reason: typing.Optional[WorkflowRunAnalysisCreditsOutReason] = None
    truncated: typing.Optional[bool] = None
    rows: typing.Optional[typing.List[WorkflowRunAnalysisCreditRowOut]] = None
    total_fractional: typing.Optional[str] = None
    unattributed_fractional: typing.Optional[str] = None
    charged: typing.Optional[int] = pydantic.Field(default=None)
    """
    Total credits charged for the run: spendable-balance debit plus separately invoiced overage credits.
    """

    overage_credits: typing.Optional[int] = None
    absorbed_fraction: typing.Optional[str] = None
    minimum_charge_fractional: typing.Optional[str] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
