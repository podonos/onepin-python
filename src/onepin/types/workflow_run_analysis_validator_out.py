# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .workflow_run_analysis_validator_locale_out import WorkflowRunAnalysisValidatorLocaleOut
from .workflow_run_analysis_validator_out_status import WorkflowRunAnalysisValidatorOutStatus


class WorkflowRunAnalysisValidatorOut(UniversalBaseModel):
    """
    One entry per validator NODE, not per kind.

    Two validators of the same kind on different generators are legal and may carry
    different thresholds, so ``node_id`` — not ``label`` — is the identity. Scores are
    read from each line's DELIVERED take (the R7 winner), which is why they can differ
    from ``/overview``'s, which reads ``audio_files[-1]`` and is frozen that way.
    """

    node_id: str
    node_type: str
    kind: str
    label: str
    threshold: typing.Optional[float] = None
    max_retries: typing.Optional[int] = None
    retry_count: typing.Optional[int] = None
    status: WorkflowRunAnalysisValidatorOutStatus
    reason: typing.Optional[str] = None
    truncated: typing.Optional[bool] = None
    unscored_locale_count: typing.Optional[int] = None
    locales: typing.Optional[typing.List[WorkflowRunAnalysisValidatorLocaleOut]] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
