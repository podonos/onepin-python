# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .workflow_run_analysis_credit_row_out_status import WorkflowRunAnalysisCreditRowOutStatus


class WorkflowRunAnalysisCreditRowOut(UniversalBaseModel):
    """
    One row per node that produced a step.

    ``credits_fractional`` is the UNROUNDED ledger value serialized as a decimal string —
    never a float, and never rounded per node. The run-level floor + minimum-1 rule is
    applied once, at settlement, and surfaces as ``charged``.

    ``status`` distinguishes the causes of an absent ledger row, which would otherwise all
    render as an indistinguishable zero or dash: ``free`` (structurally zero-cost, or a
    billable node that did no billable work), ``not_completed``, and ``missing`` — the only
    anomaly, meaning the node was reserved for, demonstrably worked, or always records an
    actual (the pronunciation corrector, whose zero is itself a measurement), yet no actual
    was ever recorded.

    ``processed_units`` is what this node was BILLED on, summed across every iteration, and
    ``unit`` says what one unit is: ``character`` for the text nodes, ``byte`` for a TTS model
    priced in UTF-8 bytes, ``word`` for the pronunciation corrector. Always render the number
    with its unit — a bare count is ambiguous, and the pair replaced a ``processed_chars``
    field whose name was true for most nodes and silently wrong for the rest.

    It is the billed quantity rather than a friendlier restatement of it: a five-character
    Korean line on a byte-priced model reads ``15`` / ``byte``, because a number sitting next
    to ``credits_fractional`` that cannot account for it does not explain the charge. Note
    that multiplying it by a single rate still will not always reproduce the credits — a
    generator spanning several providers sums groups priced differently, a translator carries
    a language multiplier, and BYOK volume counts here while costing nothing.

    ``output_chars`` is the node's final emitted text, deduped per source line and locale so a
    node that re-ran is counted once rather than once per iteration. It is not a matched pair
    with ``processed_units`` — ``retries`` is what explains the gap, and on a per-word or
    per-byte node the two are not even the same unit.

    ``retries`` counts billed re-executions and is deliberately not ``validators[].retry_count``,
    which sums per-line re-records.

    Under truncation ``output_chars`` is a partial count, consistent with ``delivered_total_ms``,
    which is likewise emitted under ``truncated``. It is NOT nulled — ``generated_total_ms`` is
    nulled on ``replaced_partial``, a different axis that truncation is not part of.

    A partial ``output_chars`` may reflect earlier-iteration text, not a prefix of the final text,
    because the union breaks before later iterations overwrite. And ``credits.truncated`` is
    block-level and cross-type: it cannot identify which row is partial.

    ``detail`` is opaque display text, not a parseable structure: ``", "`` separates both locales
    and voices, so a two-voice line and a two-locale line are identical to a parser. Render it
    as text.
    """

    node_id: str
    node_type: str
    display_name: typing.Optional[str] = None
    detail: typing.Optional[str] = None
    processed_units: typing.Optional[int] = None
    unit: typing.Optional[str] = None
    output_ms: typing.Optional[int] = None
    output_chars: typing.Optional[int] = None
    retries: typing.Optional[int] = None
    credits_fractional: typing.Optional[str] = None
    status: WorkflowRunAnalysisCreditRowOutStatus

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
