# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class ValidationDetailOut(UniversalBaseModel):
    """
    One structural or run-start rule violation.

    Mirrors the narrow subset of a raw ``ValidationDetail`` (``dict[str, object]``,
    see ``services/workflow/validator.py``) an LLM-facing caller needs to act on a
    rule. The raw dict can carry additional rule-specific keys (``edge_id``,
    ``source_type``, ...); those are intentionally dropped here rather than relayed,
    since a workaround is looked up by ``rule``, not by the extra fields.
    """

    rule: str = pydantic.Field()
    """
    Machine-readable rule identifier, e.g. `incompatible_ports`.
    """

    node_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    The node this violation is about, when the rule names one.
    """

    node_name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Display name of `node_id`, when available.
    """

    message: str = pydantic.Field()
    """
    Human-readable, localized explanation of the violation.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
