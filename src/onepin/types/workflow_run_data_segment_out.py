# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .workflow_run_data_segment_out_error_type import WorkflowRunDataSegmentOutErrorType
from .workflow_run_data_segment_out_severity import WorkflowRunDataSegmentOutSeverity
from .workflow_run_data_segment_out_status import WorkflowRunDataSegmentOutStatus


class WorkflowRunDataSegmentOut(UniversalBaseModel):
    """
    One highlighted span of a take's ``script`` from a highlight-capable validator.

    Shared by the word-accuracy (``kind == "wer"``) and pronunciation
    (``kind == "pronunciation"``) validators — one shape, two producers:

    - ``word_error`` — a reference word (WER) or character (CER) the take got wrong
      (substituted or deleted vs the STT transcript). Word-accuracy only.
    - ``pronunciation_error`` — a word the pronunciation error detector heard
      mispronounced and the corrector left uncorrected. Pronunciation only.
    - ``corrected`` — a word the pronunciation corrector re-synthesized. Pronunciation
      only.

    ``start``/``end`` are Unicode **codepoint** offsets (half-open ``[start, end)``)
    into the card's ``script`` — Python ``str`` indexing, so a JS client must slice
    via ``Array.from(script)`` (UTF-16 code units drift on astral/emoji). ``text`` is
    the script slice (``script[start:end]``) so a client can validate/fall back if its
    offset math disagrees. Only wrong/corrected units are emitted; ok text is the
    uncovered gaps. CER spans may be single characters — a client merges touching
    same-status spans.

    ``error_type`` says WHICH edit operation this one span was, on ``word_error`` spans:

    - ``substitution`` — the take said something else here. This is the mispronunciation
      case, and the only one a phoneme/pronunciation fix addresses.
    - ``deletion`` — the take dropped this unit; nothing was heard where it belongs.
      A regeneration or a text split, not a pronunciation fix.

    Two failures that a single ``word_error`` cannot tell apart, and that
    ``error_counts`` can only total per line — this is the per-word answer to "which
    word, what kind". Insertions never appear: an extra heard unit has no reference
    span in ``script`` to highlight, so it is counted (``error_counts.insertions``)
    and not painted. ``None`` on ``pronunciation_error``/``corrected`` spans (not an
    edit operation), and on ``word_error`` spans from a run scored before the
    validator recorded the type — never guessed, so absence stays absence. ``status``
    is deliberately unchanged, so a client that ignores this field renders exactly as
    before.
    """

    text: str
    status: WorkflowRunDataSegmentOutStatus
    start: int
    end: int
    severity: typing.Optional[WorkflowRunDataSegmentOutSeverity] = None
    error_type: typing.Optional[WorkflowRunDataSegmentOutErrorType] = None
    advisory: typing.Optional[bool] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
