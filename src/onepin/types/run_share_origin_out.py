# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class RunShareOriginOut(UniversalBaseModel):
    """
    Who published this run, and when.

    ``shared_by`` is the WORKSPACE name, never a person. The display-name path
    (``donut.utils.user.user_display_name``) falls back to the user's email address
    when both name fields are empty, which is not something to render on a page
    anyone can open. Note the workspace name is not automatically impersonal either:
    the default created at signup is "<given name>'s Workspace", and that is an
    accepted, recorded trade-off rather than an oversight.
    """

    shared_by: str
    shared_at: dt.datetime

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
