# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class VoicePreviewOut(UniversalBaseModel):
    locale: str = pydantic.Field()
    """
    The actual regioned locale of the selected preview tuple, not a reflection of the request's `language` parameter. A bare-family request like `?language=en` expands to both `en-us` and `en-gb`, so without this field the caller cannot know which one they are hearing.
    """

    model: str = pydantic.Field()
    """
    The TTS model the preview was synthesized with.
    """

    sample_url: str = pydantic.Field()
    """
    Time-limited presigned URL for the audio preview sample. Valid for 1 hour; regenerate by fetching the preview again.
    """

    content_type: typing.Optional[str] = pydantic.Field(default=None)
    """
    The selected row's stored `preview_content_type`, read from the database row — not a guarantee that the object in S3 is playable. May be null.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
