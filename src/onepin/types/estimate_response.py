# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .node_estimate import NodeEstimate


class EstimateResponse(UniversalBaseModel):
    """
    Pre-flight estimate range returned by POST /workflows/{id}/runs/preview.

    `min_credits` ≤ owner balance is the run-start gate (don't refuse runs that *might* fit).
    FE displays "min-max credits" or single value if min == max.
    """

    min_credits: int
    expected_credits: int
    max_credits: int
    breakdown: typing.List[NodeEstimate]
    current_balance: int
    deficit_at_max: int
    can_run: bool
    overage_rate_cents_per_credit: typing.Optional[float] = None
    will_incur_overage: typing.Optional[bool] = None
    estimated_overage_cents: typing.Optional[int] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
