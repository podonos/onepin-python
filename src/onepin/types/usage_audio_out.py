# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class UsageAudioOut(UniversalBaseModel):
    """
    Delivered audio length — the delivered take per independent generator, retries excluded.

    A different population from `characters`, which counts billed generator characters and so
    includes every regenerated take. The two are reported side by side on the Usage page but
    must not be reasoned about as one basis.

    NOT one take per line. A line fed by two independent generators — the A/B voice-comparison
    shape — delivers both branches and both count (POD-593), so such a run contributes roughly
    twice the audio a per-line reading would predict.

    A run contributes 0 until it reaches a terminal status, because the delivered total is
    stamped only then. A window covering a still-running or parked run (parked runs survive up
    to `PAUSED_RUN_MAX_DAYS`) therefore under-reports it, and the SAME window's total grows
    once that run terminates. Separately, runs that terminated before the stamp shipped to
    production (`v0.41.105`, 2026-08-07) carry no value at all, so longer `range` windows
    under-report until that date falls out of the window.
    """

    total_ms: int = pydantic.Field()
    """
    Total delivered audio length in milliseconds across the workspace in the period, counting the delivered take per independent generator on each line (retries excluded). Zero when no audio was delivered.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
