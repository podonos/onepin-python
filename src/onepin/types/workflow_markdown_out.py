# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class WorkflowMarkdownOut(UniversalBaseModel):
    """
    A workflow rendered as a markdown document, for a terminal or a chat surface.
    """

    workflow_id: str = pydantic.Field()
    """
    The workflow this document describes.
    """

    name: str = pydantic.Field()
    """
    Workflow name, also the document's H1.
    """

    markdown: str = pydantic.Field()
    """
    GitHub-flavoured markdown: a header with languages and node count, the pipeline in execution order (parallel steps shown as branches, retry loops called out separately), a node table with each node's configuration, voices, and quality gates. Text only, no diagram — print it as-is on any surface, including a terminal.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
