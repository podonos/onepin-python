# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.api_response_dict import ApiResponseDict
from ..types.api_response_upload_create_response import ApiResponseUploadCreateResponse
from ..types.api_response_upload_out import ApiResponseUploadOut
from .raw_client import AsyncRawUploadsClient, RawUploadsClient
from .types.upload_confirm_request_context_type import UploadConfirmRequestContextType
from .types.upload_request_category import UploadRequestCategory

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class UploadsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawUploadsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawUploadsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawUploadsClient
        """
        return self._raw_client

    def create(
        self,
        *,
        filename: str,
        category: UploadRequestCategory,
        workspace_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiResponseUploadCreateResponse:
        """
        Request a presigned URL for uploading a file.

        Parameters
        ----------
        filename : str

        category : UploadRequestCategory

        workspace_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseUploadCreateResponse
            Successful Response

        Examples
        --------
        from onepin import OnePinClient

        client = OnePinClient(
            token="YOUR_TOKEN",
        )
        client.uploads.create(
            filename="filename",
            category="script",
        )
        """
        _response = self._raw_client.create(
            filename=filename, category=category, workspace_id=workspace_id, request_options=request_options
        )
        return _response.data

    def confirm(
        self,
        upload_id: str,
        *,
        context_type: UploadConfirmRequestContextType,
        context_id: str,
        workspace_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiResponseUploadOut:
        """
        Confirm upload and move file to final location.

        POD-301: gates the file size against `storage_bytes_per_workspace` whenever
        the upload binds to a workspace-scoped resource (header OR derived from
        context_id). Records the storage_charge event in the same transaction as the
        upload row update.

        Parameters
        ----------
        upload_id : str

        context_type : UploadConfirmRequestContextType

        context_id : str

        workspace_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseUploadOut
            Successful Response

        Examples
        --------
        from onepin import OnePinClient

        client = OnePinClient(
            token="YOUR_TOKEN",
        )
        client.uploads.confirm(
            upload_id="upload_id",
            context_type="workflow",
            context_id="context_id",
        )
        """
        _response = self._raw_client.confirm(
            upload_id,
            context_type=context_type,
            context_id=context_id,
            workspace_id=workspace_id,
            request_options=request_options,
        )
        return _response.data

    def delete(
        self,
        upload_id: str,
        *,
        workspace_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiResponseDict:
        """
        Delete an upload and its S3 object.

        DB record is deleted first (committed on response). S3 cleanup runs
        after the response via a background task so the file is only removed
        once the DB commit succeeds.

        POD-301: if the upload was confirmed against a workspace-scoped resource,
        release the bytes back to that workspace's storage counter. Without this,
        storage_bytes_used drifts upward forever and customers stay capped after
        deleting files. Read upload state BEFORE delete_for_user — the row is gone
        after that call.

        Parameters
        ----------
        upload_id : str

        workspace_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseDict
            Successful Response

        Examples
        --------
        from onepin import OnePinClient

        client = OnePinClient(
            token="YOUR_TOKEN",
        )
        client.uploads.delete(
            upload_id="upload_id",
        )
        """
        _response = self._raw_client.delete(upload_id, workspace_id=workspace_id, request_options=request_options)
        return _response.data


class AsyncUploadsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawUploadsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawUploadsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawUploadsClient
        """
        return self._raw_client

    async def create(
        self,
        *,
        filename: str,
        category: UploadRequestCategory,
        workspace_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiResponseUploadCreateResponse:
        """
        Request a presigned URL for uploading a file.

        Parameters
        ----------
        filename : str

        category : UploadRequestCategory

        workspace_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseUploadCreateResponse
            Successful Response

        Examples
        --------
        import asyncio

        from onepin import AsyncOnePinClient

        client = AsyncOnePinClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.uploads.create(
                filename="filename",
                category="script",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            filename=filename, category=category, workspace_id=workspace_id, request_options=request_options
        )
        return _response.data

    async def confirm(
        self,
        upload_id: str,
        *,
        context_type: UploadConfirmRequestContextType,
        context_id: str,
        workspace_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiResponseUploadOut:
        """
        Confirm upload and move file to final location.

        POD-301: gates the file size against `storage_bytes_per_workspace` whenever
        the upload binds to a workspace-scoped resource (header OR derived from
        context_id). Records the storage_charge event in the same transaction as the
        upload row update.

        Parameters
        ----------
        upload_id : str

        context_type : UploadConfirmRequestContextType

        context_id : str

        workspace_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseUploadOut
            Successful Response

        Examples
        --------
        import asyncio

        from onepin import AsyncOnePinClient

        client = AsyncOnePinClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.uploads.confirm(
                upload_id="upload_id",
                context_type="workflow",
                context_id="context_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.confirm(
            upload_id,
            context_type=context_type,
            context_id=context_id,
            workspace_id=workspace_id,
            request_options=request_options,
        )
        return _response.data

    async def delete(
        self,
        upload_id: str,
        *,
        workspace_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiResponseDict:
        """
        Delete an upload and its S3 object.

        DB record is deleted first (committed on response). S3 cleanup runs
        after the response via a background task so the file is only removed
        once the DB commit succeeds.

        POD-301: if the upload was confirmed against a workspace-scoped resource,
        release the bytes back to that workspace's storage counter. Without this,
        storage_bytes_used drifts upward forever and customers stay capped after
        deleting files. Read upload state BEFORE delete_for_user — the row is gone
        after that call.

        Parameters
        ----------
        upload_id : str

        workspace_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseDict
            Successful Response

        Examples
        --------
        import asyncio

        from onepin import AsyncOnePinClient

        client = AsyncOnePinClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.uploads.delete(
                upload_id="upload_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(upload_id, workspace_id=workspace_id, request_options=request_options)
        return _response.data
