# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.api_list_response_workspace_member_out import ApiListResponseWorkspaceMemberOut
from ..types.api_response_dict import ApiResponseDict
from ..types.api_response_workspace_invite_out import ApiResponseWorkspaceInviteOut
from ..types.workspace_role import WorkspaceRole
from .raw_client import AsyncRawWorkspaceMembersClient, RawWorkspaceMembersClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class WorkspaceMembersClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawWorkspaceMembersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawWorkspaceMembersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawWorkspaceMembersClient
        """
        return self._raw_client

    def list_members(
        self, ws_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiListResponseWorkspaceMemberOut:
        """
        List active members and pending invites for a workspace.

        Returns a unified list combining confirmed members (status `active`) and
        outstanding invites that have not yet been accepted or revoked (status
        `invited`). Pending invites appear with `user_id: null` and only the
        `email` and `role` fields populated.

        The list is sorted: the requesting user appears first, then admins by
        join date, then other members by join date. No pagination — the full
        roster is returned in a single response.

        Roles:
        - `admin`: can manage members, invites, workspace settings, and all content.
        - `editor`: can create, edit, and run workflows; cannot manage members.
        - `viewer`: read-only access to workspace content and run history.

        Parameters
        ----------
        ws_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiListResponseWorkspaceMemberOut
            Successful Response

        Examples
        --------
        from onepin import OnePinClient

        client = OnePinClient(
            token="YOUR_TOKEN",
        )
        client.workspace_members.list_members(
            ws_id="ws_id",
        )
        """
        _response = self._raw_client.list_members(ws_id, request_options=request_options)
        return _response.data

    def create_invite(
        self, ws_id: str, *, email: str, role: WorkspaceRole, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiResponseWorkspaceInviteOut:
        """
        Invite a user to the workspace by email. Admin only.

        Creates a pending invite and sends an invitation email to the specified
        address. The invitee does not need to have an existing account — they can
        sign up after receiving the invite. The invite includes a role
        (`admin`, `editor`, or `viewer`) that the invitee will receive upon
        accepting.

        Invites expire after 14 days. Only one pending invite per email address
        per workspace is allowed at a time; re-inviting the same address while a
        pending invite exists returns 409. Inviting an address that already
        belongs to an active member also returns 409.

        The total number of active members plus pending invites is counted against
        the workspace owner's plan seat limit. Exceeding the limit returns 402.
        The invitee's role can be updated before acceptance via
        `PATCH /workspaces/{ws_id}/invites/{invite_id}`, or the invite can be
        cancelled via `DELETE /workspaces/{ws_id}/invites/{invite_id}`.

        Parameters
        ----------
        ws_id : str

        email : str
            Email address to invite. Normalized to lowercase. Returns 409 if this address is already an active member or has a pending invite.

        role : WorkspaceRole
            Role to grant when the invite is accepted: `admin`, `editor`, or `viewer`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseWorkspaceInviteOut
            Successful Response

        Examples
        --------
        from onepin import OnePinClient

        client = OnePinClient(
            token="YOUR_TOKEN",
        )
        client.workspace_members.create_invite(
            ws_id="ws_id",
            email="email",
            role="admin",
        )
        """
        _response = self._raw_client.create_invite(ws_id, email=email, role=role, request_options=request_options)
        return _response.data

    def remove_member(
        self, ws_id: str, member_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiResponseDict:
        """
        Remove an active member from the workspace. Admin only.

        The removed member immediately loses access to all workspace resources.
        They receive an email notification informing them they have been removed.

        Two protections prevent accidental lockouts:
        - The workspace owner cannot be removed.
        - The last remaining admin cannot be removed (returns 409).

        Removing a member does not affect their account or other workspaces. To
        block an invited but not-yet-accepted user instead, revoke the invite via
        `DELETE /workspaces/{ws_id}/invites/{invite_id}`.

        Parameters
        ----------
        ws_id : str

        member_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseDict
            Successful Response

        Examples
        --------
        from onepin import OnePinClient

        client = OnePinClient(
            token="YOUR_TOKEN",
        )
        client.workspace_members.remove_member(
            ws_id="ws_id",
            member_id="member_id",
        )
        """
        _response = self._raw_client.remove_member(ws_id, member_id, request_options=request_options)
        return _response.data

    def update_member_role(
        self,
        ws_id: str,
        member_id: str,
        *,
        role: WorkspaceRole,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiResponseDict:
        """
        Change a workspace member's role. Admin only.

        Updates the role of an active member to `admin`, `editor`, or `viewer`.
        The operation is idempotent — setting a member to their current role
        succeeds silently (no error, no duplicate email notification).

        Two protections prevent accidental lockouts:
        - The workspace owner's role cannot be changed.
        - The last remaining admin cannot be demoted (returns 409).

        When the role actually changes, the affected member receives an email
        notification describing their new permissions.

        Parameters
        ----------
        ws_id : str

        member_id : str

        role : WorkspaceRole
            New role to assign: `admin`, `editor`, or `viewer`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseDict
            Successful Response

        Examples
        --------
        from onepin import OnePinClient

        client = OnePinClient(
            token="YOUR_TOKEN",
        )
        client.workspace_members.update_member_role(
            ws_id="ws_id",
            member_id="member_id",
            role="admin",
        )
        """
        _response = self._raw_client.update_member_role(ws_id, member_id, role=role, request_options=request_options)
        return _response.data

    def revoke_invite(
        self, ws_id: str, invite_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiResponseDict:
        """
        Cancel a pending invite so the invitee can no longer accept it. Admin only.

        The invite token is invalidated immediately. If the invitee attempts to
        accept after revocation, they receive a 410 Gone. The invite is removed
        from the pending list returned by `GET /workspaces/{ws_id}/members`.

        Revoking an invite that is already accepted, revoked, or expired returns
        404. To remove an already-accepted member, use
        `DELETE /workspaces/{ws_id}/members/{member_id}`.

        Parameters
        ----------
        ws_id : str

        invite_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseDict
            Successful Response

        Examples
        --------
        from onepin import OnePinClient

        client = OnePinClient(
            token="YOUR_TOKEN",
        )
        client.workspace_members.revoke_invite(
            ws_id="ws_id",
            invite_id="invite_id",
        )
        """
        _response = self._raw_client.revoke_invite(ws_id, invite_id, request_options=request_options)
        return _response.data

    def update_invite_role(
        self,
        ws_id: str,
        invite_id: str,
        *,
        role: WorkspaceRole,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiResponseWorkspaceInviteOut:
        """
        Change the role on a pending (not yet accepted) invite. Admin only.

        Updates the role the invitee will receive when they accept. Only pending
        invites can be updated — attempting to update an accepted, revoked, or
        expired invite returns 409. The invitee is not notified of the role
        change; the updated role takes effect when they accept.

        Parameters
        ----------
        ws_id : str

        invite_id : str

        role : WorkspaceRole
            New role to assign: `admin`, `editor`, or `viewer`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseWorkspaceInviteOut
            Successful Response

        Examples
        --------
        from onepin import OnePinClient

        client = OnePinClient(
            token="YOUR_TOKEN",
        )
        client.workspace_members.update_invite_role(
            ws_id="ws_id",
            invite_id="invite_id",
            role="admin",
        )
        """
        _response = self._raw_client.update_invite_role(ws_id, invite_id, role=role, request_options=request_options)
        return _response.data

    def accept_invite(self, token: str, *, request_options: typing.Optional[RequestOptions] = None) -> ApiResponseDict:
        """
        Accept a workspace invite using the token from the invitation email.

        The `token` path parameter comes from the invitation link sent to the
        invitee's email. The caller must be authenticated and their verified email
        address must match the address the invite was sent to (403 if it does not).

        On success the caller is added to the workspace with the role specified in
        the invite, and `workspace_id` is returned so the caller can immediately
        begin using that workspace. If the caller is already a member of the
        workspace (e.g. accepted via a different device), the accept is idempotent
        and returns the same `workspace_id`.

        Error cases (all return 410 Gone):
        - Invite already accepted.
        - Invite was revoked by an admin.
        - Invite has expired (14-day TTL from creation).

        The workspace owner's plan seat limit is re-checked at accept time in case
        the plan was downgraded after the invite was sent; exceeding the limit
        returns 402.

        Parameters
        ----------
        token : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseDict
            Successful Response

        Examples
        --------
        from onepin import OnePinClient

        client = OnePinClient(
            token="YOUR_TOKEN",
        )
        client.workspace_members.accept_invite(
            token="token",
        )
        """
        _response = self._raw_client.accept_invite(token, request_options=request_options)
        return _response.data


class AsyncWorkspaceMembersClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawWorkspaceMembersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawWorkspaceMembersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawWorkspaceMembersClient
        """
        return self._raw_client

    async def list_members(
        self, ws_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiListResponseWorkspaceMemberOut:
        """
        List active members and pending invites for a workspace.

        Returns a unified list combining confirmed members (status `active`) and
        outstanding invites that have not yet been accepted or revoked (status
        `invited`). Pending invites appear with `user_id: null` and only the
        `email` and `role` fields populated.

        The list is sorted: the requesting user appears first, then admins by
        join date, then other members by join date. No pagination — the full
        roster is returned in a single response.

        Roles:
        - `admin`: can manage members, invites, workspace settings, and all content.
        - `editor`: can create, edit, and run workflows; cannot manage members.
        - `viewer`: read-only access to workspace content and run history.

        Parameters
        ----------
        ws_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiListResponseWorkspaceMemberOut
            Successful Response

        Examples
        --------
        import asyncio

        from onepin import AsyncOnePinClient

        client = AsyncOnePinClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.workspace_members.list_members(
                ws_id="ws_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_members(ws_id, request_options=request_options)
        return _response.data

    async def create_invite(
        self, ws_id: str, *, email: str, role: WorkspaceRole, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiResponseWorkspaceInviteOut:
        """
        Invite a user to the workspace by email. Admin only.

        Creates a pending invite and sends an invitation email to the specified
        address. The invitee does not need to have an existing account — they can
        sign up after receiving the invite. The invite includes a role
        (`admin`, `editor`, or `viewer`) that the invitee will receive upon
        accepting.

        Invites expire after 14 days. Only one pending invite per email address
        per workspace is allowed at a time; re-inviting the same address while a
        pending invite exists returns 409. Inviting an address that already
        belongs to an active member also returns 409.

        The total number of active members plus pending invites is counted against
        the workspace owner's plan seat limit. Exceeding the limit returns 402.
        The invitee's role can be updated before acceptance via
        `PATCH /workspaces/{ws_id}/invites/{invite_id}`, or the invite can be
        cancelled via `DELETE /workspaces/{ws_id}/invites/{invite_id}`.

        Parameters
        ----------
        ws_id : str

        email : str
            Email address to invite. Normalized to lowercase. Returns 409 if this address is already an active member or has a pending invite.

        role : WorkspaceRole
            Role to grant when the invite is accepted: `admin`, `editor`, or `viewer`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseWorkspaceInviteOut
            Successful Response

        Examples
        --------
        import asyncio

        from onepin import AsyncOnePinClient

        client = AsyncOnePinClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.workspace_members.create_invite(
                ws_id="ws_id",
                email="email",
                role="admin",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_invite(ws_id, email=email, role=role, request_options=request_options)
        return _response.data

    async def remove_member(
        self, ws_id: str, member_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiResponseDict:
        """
        Remove an active member from the workspace. Admin only.

        The removed member immediately loses access to all workspace resources.
        They receive an email notification informing them they have been removed.

        Two protections prevent accidental lockouts:
        - The workspace owner cannot be removed.
        - The last remaining admin cannot be removed (returns 409).

        Removing a member does not affect their account or other workspaces. To
        block an invited but not-yet-accepted user instead, revoke the invite via
        `DELETE /workspaces/{ws_id}/invites/{invite_id}`.

        Parameters
        ----------
        ws_id : str

        member_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseDict
            Successful Response

        Examples
        --------
        import asyncio

        from onepin import AsyncOnePinClient

        client = AsyncOnePinClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.workspace_members.remove_member(
                ws_id="ws_id",
                member_id="member_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.remove_member(ws_id, member_id, request_options=request_options)
        return _response.data

    async def update_member_role(
        self,
        ws_id: str,
        member_id: str,
        *,
        role: WorkspaceRole,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiResponseDict:
        """
        Change a workspace member's role. Admin only.

        Updates the role of an active member to `admin`, `editor`, or `viewer`.
        The operation is idempotent — setting a member to their current role
        succeeds silently (no error, no duplicate email notification).

        Two protections prevent accidental lockouts:
        - The workspace owner's role cannot be changed.
        - The last remaining admin cannot be demoted (returns 409).

        When the role actually changes, the affected member receives an email
        notification describing their new permissions.

        Parameters
        ----------
        ws_id : str

        member_id : str

        role : WorkspaceRole
            New role to assign: `admin`, `editor`, or `viewer`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseDict
            Successful Response

        Examples
        --------
        import asyncio

        from onepin import AsyncOnePinClient

        client = AsyncOnePinClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.workspace_members.update_member_role(
                ws_id="ws_id",
                member_id="member_id",
                role="admin",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_member_role(
            ws_id, member_id, role=role, request_options=request_options
        )
        return _response.data

    async def revoke_invite(
        self, ws_id: str, invite_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiResponseDict:
        """
        Cancel a pending invite so the invitee can no longer accept it. Admin only.

        The invite token is invalidated immediately. If the invitee attempts to
        accept after revocation, they receive a 410 Gone. The invite is removed
        from the pending list returned by `GET /workspaces/{ws_id}/members`.

        Revoking an invite that is already accepted, revoked, or expired returns
        404. To remove an already-accepted member, use
        `DELETE /workspaces/{ws_id}/members/{member_id}`.

        Parameters
        ----------
        ws_id : str

        invite_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseDict
            Successful Response

        Examples
        --------
        import asyncio

        from onepin import AsyncOnePinClient

        client = AsyncOnePinClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.workspace_members.revoke_invite(
                ws_id="ws_id",
                invite_id="invite_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.revoke_invite(ws_id, invite_id, request_options=request_options)
        return _response.data

    async def update_invite_role(
        self,
        ws_id: str,
        invite_id: str,
        *,
        role: WorkspaceRole,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApiResponseWorkspaceInviteOut:
        """
        Change the role on a pending (not yet accepted) invite. Admin only.

        Updates the role the invitee will receive when they accept. Only pending
        invites can be updated — attempting to update an accepted, revoked, or
        expired invite returns 409. The invitee is not notified of the role
        change; the updated role takes effect when they accept.

        Parameters
        ----------
        ws_id : str

        invite_id : str

        role : WorkspaceRole
            New role to assign: `admin`, `editor`, or `viewer`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseWorkspaceInviteOut
            Successful Response

        Examples
        --------
        import asyncio

        from onepin import AsyncOnePinClient

        client = AsyncOnePinClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.workspace_members.update_invite_role(
                ws_id="ws_id",
                invite_id="invite_id",
                role="admin",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_invite_role(
            ws_id, invite_id, role=role, request_options=request_options
        )
        return _response.data

    async def accept_invite(
        self, token: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ApiResponseDict:
        """
        Accept a workspace invite using the token from the invitation email.

        The `token` path parameter comes from the invitation link sent to the
        invitee's email. The caller must be authenticated and their verified email
        address must match the address the invite was sent to (403 if it does not).

        On success the caller is added to the workspace with the role specified in
        the invite, and `workspace_id` is returned so the caller can immediately
        begin using that workspace. If the caller is already a member of the
        workspace (e.g. accepted via a different device), the accept is idempotent
        and returns the same `workspace_id`.

        Error cases (all return 410 Gone):
        - Invite already accepted.
        - Invite was revoked by an admin.
        - Invite has expired (14-day TTL from creation).

        The workspace owner's plan seat limit is re-checked at accept time in case
        the plan was downgraded after the invite was sent; exceeding the limit
        returns 402.

        Parameters
        ----------
        token : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApiResponseDict
            Successful Response

        Examples
        --------
        import asyncio

        from onepin import AsyncOnePinClient

        client = AsyncOnePinClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.workspace_members.accept_invite(
                token="token",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.accept_invite(token, request_options=request_options)
        return _response.data
