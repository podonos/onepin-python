"""OnePin Python SDK + CLI.

The SDK surface is generated by Fern from the OpenAPI spec at
podonos/onepin-sdks; CLI is hand-rolled atop the generated client.
Until the first Fern regen lands, the public client classes are not
yet available -- only the CLI module is usable.
"""

from __future__ import annotations

from importlib.metadata import PackageNotFoundError, version as _pkg_version

try:
    __version__ = _pkg_version("onepin")
except PackageNotFoundError:  # pragma: no cover -- happens in editable install pre-build
    __version__ = "0.0.0+local"

__all__ = ["__version__"]
